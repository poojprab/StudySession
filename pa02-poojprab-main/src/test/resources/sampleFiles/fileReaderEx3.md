-- TEST FILE: should output: "only creates a reference no array has actually been created yet"

- The above [[only creates a reference]] to an array object, but [[no array has
  actually been created yet]].  