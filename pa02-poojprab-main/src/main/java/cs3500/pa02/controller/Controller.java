package cs3500.pa02.controller;

/**
 * Interface that separates the controller classes
 */
public interface Controller {

  /**
   * Handles programs that take in input and act accordingly (console input and command
   * line input)
   */
  void run() throws Exception;
}
